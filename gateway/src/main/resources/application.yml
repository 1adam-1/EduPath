server:
  port: 8888

spring:
  application:
    name: gateway
  cloud:
    gateway:
      httpclient:
        connect-timeout: 5000
        response-timeout: 10s
      server:
        webmvc:
          routes:
            - id: backend_route
              uri: http://localhost:8082
              predicates:
                - Path=/api/**
            - id: ai_service_route
              uri: http://localhost:8000
              predicates:
                - Path=/ai/**
